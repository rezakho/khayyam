language: php

php:
  - 5.3
  - 5.4
  - 5.5
  - 5.6
  - 7
  - hhvm
  - nightly


matrix:
  allow_failures:
    - php: 7
    - php: nightly

sudo: false

before_install:
  - travis_retry composer self-update

install: travis_retry composer install --no-interaction --prefer-source

script: phpunit